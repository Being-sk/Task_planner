# Frontend — Task Plan Prototype

This is a Vite + React frontend.

Run

```bash
cd frontend
npm install
npm run dev
```

By default it calls `http://localhost:4000` for the API. Set `VITE_API_BASE` to change.

Example:

```bash
VITE_API_BASE=http://localhost:4000 npm run dev
```
