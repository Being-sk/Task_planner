# Example environment variables for backend
# Copy to a .env file or set in your environment

# OpenAI API key (optional). When set, the backend will attempt to produce a JSON plan using the provider.
OPENAI_API_KEY=
# Optional: override model name
OPENAI_MODEL=gpt-4o-mini
